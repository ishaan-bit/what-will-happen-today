{
  "name": "wwht-local-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local worker for What Will Happen Today — runs Ollama LLM jobs that the Vercel backend cannot self-trigger.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "generate": "node generate.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.9",
    "dotenv": "^16.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
